import ReactDOM from 'react-dom/client';
import App from './App';

// Initialized from app creation
const root = ReactDOM.createRoot(
  document.getElementById('root') as HTMLElement
);
root.render(<App />);
